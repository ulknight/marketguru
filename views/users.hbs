<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Страница пользователя</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>

  </head>
  <body>
    <div class="container">
     <h4 class="main-title">Страница: {{ page }}</h4>

     <div class="row">
          <div class="col-6">
              <form action="/users/all/{{ page }}" method="POST">
                  <div class="input-group mb-3">
                    <button class="btn btn-primary" type="submit" id="button-addon1">Search</button>
                    <input type="text" name="search" value="{{search}}" class="form-control" placeholder="" aria-label="Example text with button addon" aria-describedby="button-addon1">
                  </div>
              </form>
              <table class="table">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">Email</th>
                    <th scope="col">Phone</th>
                    <th scope="col">Name</th>
                    <th scope="col">Age</th>
                  </tr>
                </thead>
                <tbody>
                  {{#each users}}
                  <tr>
                    <th scope="row">{{id}}</th>
                    <td>{{email}}</td>
                    <td>{{phone}}</td>
                    <td>{{name}}</td>
                    <td>{{age}}</td>
                  </tr>
                  {{/each}}
                </tbody>
              </table>
          </div>

      </div>
      {{#if prev_page}}<a class="btn btn-primary" href="/users/all/{{prev_page}}/{{search}}" role="button">Prev</a> {{/if}}
                <a class="btn btn-primary" href="/users/all/{{next_page}}/{{search}}" role="button">Next</a>
    </div>
  </body>
</html>